function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    /* eslint-diable */
    if (true) {
      resolve();
    } else {
      reject();
    }
    /* eslint-disable */
  });
}

export default getResponseFromAPI;
